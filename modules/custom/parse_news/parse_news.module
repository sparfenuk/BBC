<?php

/**
 * @file
 * Primary module hooks for Parse news module.
 */

/**
 * Implements hook_theme().
 */
function parse_news_theme($existing, $type, $theme, $path) {
  return [
    'parse_news' => [
      'variables' => ['items' => [], 'title' => 'Parse news']
    ]
  ];
}
